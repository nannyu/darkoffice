# 项目开发规范

## 1. 文档目的与适用范围

本文档是《暗黑职场》项目当前阶段的开发与协作总规范，适用于：

- 项目负责人
- 未来的人类协作者
- 参与项目推进的 AI/Agent
- 后续任何实现、验证、重构与文档迁移工作

本文档解决的是“如何有秩序地推进项目”，而不是“当前具体采用哪种技术栈实现”。

## 2. 当前项目阶段与开发原则

### 2.1 当前阶段定义

当前处于 **M7（内容规模化生产）** 阶段。M0-M4（文档建设）和 M5-M6（最小运行闭环 + 素材库 + 剧情线）已全部完成。

技术栈已定（Python 3.12 + SQLite + TypeScript adapter），核心运行闭环已实现并经过代码审计。当前阶段重点为内容规模化（蒸馏卡牌、编排剧情线）与平衡性验证。

### 2.2 核心开发原则

1. 先文档，后实现。
2. 先定义边界，后扩展细节。
3. 先最小闭环，后丰富内容。
4. 先单一事实源，后多处引用。
5. 先验证规则，后堆砌功能。
6. 先保守假设，后技术定案。

### 2.3 当前阶段允许的工作

- 从素材库蒸馏生成角色卡、事件卡、隐患卡
- 编排多幕剧情线并测试剧情模式运行
- 运行平衡性模拟、调优数值参数
- 基于模板批量生产事件卡/角色卡
- 修复引擎 bug 与补充边界用例
- 多 Agent 框架迭代与功能接入

### 2.4 当前阶段禁止的工作

- 跳过平衡性验证直接发布内容
- 添加没有验收标准的新功能
- 在多个文档中平行定义同一规则
- 绕过事实源直接修改引擎核心规则

## 3. 单一事实源与文档优先级

### 3.1 单一事实源原则

每类信息只能有一个正式归属位置。任何新共识都应写入对应事实源，而不是散落在聊天记录、临时草稿或重复文档中。

### 3.2 当前文档优先级

1. `docs/project/`：项目定位、范围、阶段目标、术语的事实源
2. `docs/design/`：玩法体验与整体设计的事实源
3. `docs/systems/`：系统规则、流程和参数定义的事实源
4. `docs/content/`：内容模板、角色池、事件池和文本规范的事实源
5. `docs/collaboration/development-guidelines.md`：开发与协作规范事实源
6. `docs/INDEX.md`：导航入口与阅读顺序事实源
7. 根目录历史文档：历史参考，不作为当前最高优先级依据

### 3.3 冲突处理规则

- 当结构化子文档与历史总案冲突时，以结构化子文档为准。
- 当导航文档与正式规范冲突时，以正式规范为准。
- 当多个子文档发生冲突时，必须新增一次文档修订，消除冲突后再继续推进。
- 未写入事实源的口头结论，默认不视为项目共识。

## 4. 仓库结构与命名规范

### 4.1 根目录规则

- 根目录只保留入口级文件与少量历史文档
- 所有正式文档统一进入 `docs/`
- 不在根目录堆放大量临时文档、讨论稿和版本副本

### 4.2 文档目录规则

```text
docs/
├── project/
├── design/
├── systems/
├── content/
├── collaboration/
└── archive/
```

各目录含义：

- `project/`：项目层定义
- `design/`：玩法与体验设计
- `systems/`：可执行规则
- `content/`：内容资产与模板
- `collaboration/`：流程、规范、交接
- `archive/`：历史资料与迁移说明

### 4.3 文件命名规则

- 正式文档文件名优先使用英文 `kebab-case`
- 文档标题可以使用中文
- 避免在正式事实源中持续新增中文文件名、空格文件名和版本号后缀文件名
- 不使用 `final`、`最新版`、`修订版2` 这类不可维护命名

推荐示例：

- `core-loop.md`
- `card-system.md`
- `event-generation.md`
- `agent-handoff.md`

不推荐示例：

- `新版本最终版.md`
- `系统文档 真的最终版.md`
- `卡牌系统V1.2-修正版.md`

## 5. 任务拆分与迭代节奏

### 5.1 任务拆分原则

每次推进只解决一个清晰问题。任务必须可以明确回答以下问题：

- 本次改动解决什么问题
- 影响哪些文档或模块
- 产出是什么
- 如何验证已完成

### 5.2 推荐任务粒度

推荐将任务控制在以下粒度：

- 一次只完善一个主题文档
- 一次只迁移一个事实源
- 一次只解决一类冲突或缺口
- 一次只做一个闭环，不并行推进多个模糊目标

### 5.3 当前阶段的迭代节奏

M7 阶段按以下节奏推进：

1. 素材导入（外部案例/经历 → materials 表）
2. 卡牌蒸馏（素材 → 角色卡/事件卡/隐患卡）
3. 剧情线编排（多幕结构 + 分支 + 结局）
4. 平衡性模拟（`simulate_balance.py` → 生存率统计）
5. 数值调优（根据模拟结果调整规则参数）
6. 验证闭环（`verify_skill.sh` → 全流程通过）

## 6. 变更流程与提交要求

### 6.1 变更前要求

任何开发或设计动作前，必须先确认：

- 当前变更对应的事实源文档在哪里
- 是否会与已有文档冲突
- 是否需要先补文档再继续

### 6.2 标准变更流程

1. 确认目标与边界
2. 找到对应事实源
3. 更新事实源文档
4. 更新索引或迁移说明
5. 做一致性检查
6. 记录本次变更影响

### 6.3 提交信息要求

未来启用版本控制后，提交信息应遵循：

- 说明本次变更主题
- 说明变更类型
- 避免使用无意义提交说明

推荐格式：

- `docs: define development guidelines`
- `docs: split card system into systems section`
- `docs: migrate legacy planning references`

不推荐格式：

- `update`
- `fix`
- `again`

## 7. 文档更新规范

### 7.1 更新规则

- 修改规则时，必须直接修改对应事实源
- 修改事实源后，必要时同步更新 `docs/INDEX.md`
- 不允许只改摘要，不改正式定义
- 不允许在多个地方复制同一段规则后分别演化

### 7.2 文档写作要求

- 优先写结论，再写解释
- 使用稳定术语，避免同义词乱跳
- 规则、流程、字段尽量结构化表达
- 说明边界时，要明确“包含什么”和“不包含什么”
- 技术未定项必须明确标注“待定”，不能伪装成已定方案

### 7.3 文档成熟度标记

建议后续为核心文档增加成熟度状态：

- `draft`：草稿，允许较大调整
- `reviewing`：评审中，尽量避免结构性改写
- `stable`：稳定，可作为实现依据
- `archived`：归档，不再继续维护

## 8. 测试与验证原则

### 8.1 总原则

技术栈已定，验证与实现并行推进。任何结论都应满足”可检查、可复述、可追踪”。

### 8.2 文档阶段的验证方式

- 检查是否存在重复定义
- 检查是否存在术语冲突
- 检查章节是否覆盖当前目标
- 检查规则是否自洽
- 检查是否存在未标注的假设

### 8.3 开发阶段的验证底线

当前已进入开发阶段，验证底线如下：

- 新功能必须有明确验收标准
- 修复性改动必须有复现路径与验证结果
- 任何“已完成”声明都应有可验证证据支撑
- 文档、规则、实现三者出现偏差时，必须回到事实源对齐

## 9. 技术实现预留规范

### 9.1 当前状态

技术栈已定：**Python 3.12 + SQLite**（后端引擎）+ **TypeScript**（适配层 + Agent 框架）。已落地实现。

### 9.2 技术定案结论

以下问题在 M5-M6 阶段已全部回答：

- 项目首个可运行原型：CLI 驱动的卡牌回合制游戏（`game_state_cli.py`）
- 数据管理：SQLite 持久化（5 表：game_sessions, turn_logs, materials, custom_cards, storylines）
- 内容生产与运行共享数据结构：自定义卡牌通过 `materials.py` 管理，运行时通过 `merge_characters/merge_events` 合并
- AI/Agent 角色：作为游戏主持人（Agent Skill）+ 多 Agent 决策辅助（Game/Analytics/Risk/Execution）

### 9.3 技术实现通用约束

- 先围绕最小闭环设计目录，而不是为未来所有可能性过度预埋
- 模块边界必须能映射回文档中的系统边界
- 规则数据应尽量与表现层解耦
- 可配置内容优先结构化，不硬编码在表现逻辑中
- 临时技术决策必须记录理由和影响范围

## 10. AI/Agent 协作规范

### 10.1 开始任务前的最小读取集

AI/Agent 在开始任何任务前，至少应读取：

1. `README.md`
2. `docs/INDEX.md`
3. 本次任务对应的事实源文档
4. `docs/collaboration/development-guidelines.md`

### 10.2 AI/Agent 工作要求

- 先确认当前任务属于哪个文档域
- 先找事实源，再做新增内容
- 不根据单一历史文档直接扩写全局规则
- 做出假设时必须显式说明
- 发现冲突时先停下并回到文档层处理

### 10.3 AI/Agent 交接要求

交接内容至少包括：

- 本次修改了什么
- 修改依据是什么
- 哪些问题已经解决
- 哪些问题仍待确认
- 下一步建议看哪份文档

## 11. 禁止事项

- 禁止绕过文档直接定义项目核心规则
- 禁止多个文档重复定义同一事实且长期不清理
- 禁止把历史草稿当作当前最高优先级依据
- 禁止在技术栈未定时伪造工程约束（~~已定案~~）
- 禁止为了”先做起来”而跳过验收定义与平衡性验证
- 禁止把聊天结论当作正式共识却不落文档

## 12. 附录模板

### 12.1 文档变更记录模板

```markdown
## 变更记录

- 日期：
- 变更文档：
- 变更目的：
- 影响范围：
- 是否涉及事实源迁移：
- 待确认事项：
```

### 12.2 任务卡模板

```markdown
## 任务卡

- 任务名称：
- 任务类型：文档建设 / 规则整理 / 内容补全 / 技术预研
- 对应事实源：
- 输入资料：
- 预期产出：
- 完成标准：
- 风险与假设：
```

### 12.3 交接模板

```markdown
## 交接摘要

- 已完成：
- 已更新文档：
- 关键决定：
- 未解决问题：
- 下一步建议：
```
