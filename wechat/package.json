{
  "name": "darkoffice-wechat",
  "version": "0.1.0-alpha",
  "private": true,
  "description": "暗黑职场微信小程序",
  "scripts": {
    "tsc": "tsc -p tsconfig.json --noEmit",
    "build:miniprogram": "tsc -p tsconfig.json",
    "build:cloudfunctions": "tsc -p tsconfig.cloudfunctions.json"
  },
  "devDependencies": {
    "@types/node": "^25.6.0",
    "miniprogram-api-typings": "^3.12.0",
    "typescript": "^6.0.3"
  }
}
